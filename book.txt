Contens list:

1. Git basic commands
        - git init
        - git stastus
        - git add
        - git rm --cached
        - git commit
        - git commit --ammend
        - git log
	- git checkout -- book.txt
	- git diff
	- git reset HEAD book.txt
	- git stash
	- git checkout -b branch_nam
	- git stash pop --index 0
	- git checkout master
	- git merge (branch_name || orgin/branch_name) 
	- git breanch -d branch_name 
	- git branch 
	- git remote -listowanie 
	- git remote  -v ( Wypisywaie razem z adresami)
	- git add  remote (nazwa) (adres url )
	- git remote rm (nazwa)
